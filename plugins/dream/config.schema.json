{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "enabled": {
      "type": "boolean",
      "title": "启用梦境整理"
    },
    "life_reflection_enabled": {
      "type": "boolean",
      "title": "启用夜间生命反思",
      "description": "默认关闭。开启后 Dream 会复用夜间整理循环，将当天日程、active story arc 和群聊片段反思为经历洞察卡。"
    },
    "interval_hours": {
      "type": "integer",
      "title": "整理间隔小时",
      "minimum": 1
    },
    "max_rounds": {
      "type": "integer",
      "title": "最大工具轮数",
      "minimum": 1,
      "maximum": 50
    },
    "sticker_delete_floor": {
      "type": "integer",
      "title": "表情库删除下限",
      "description": "库存 ≤ 该值时 Dream 不再淘汰表情包，防止单向缩水。",
      "minimum": 0,
      "maximum": 5000
    }
  }
}
