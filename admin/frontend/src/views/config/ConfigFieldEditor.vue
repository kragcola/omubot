<script setup lang="ts">
/**
 * @deprecated This component has been replaced by `ConfigField.vue` and its
 * dedicated subcomponents (ConfigListField / ConfigKvField / ConfigObjectGroup
 * / ConfigSecretInput / ConfigJsonInput). Kept as an empty placeholder so the
 * filename does not get re-imported by any stale reference. Safe to delete
 * once you confirm nothing else still imports it.
 */
defineOptions({ name: 'ConfigFieldEditor' })
</script>

<template>
  <span style="display: none" />
</template>
